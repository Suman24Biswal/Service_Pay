import PayMethod from "../../WaterPage/PatMethod/PayMethod"

function GasPayMethod({title}) {
    
  return (
    <div>
        <PayMethod title={title}/>
    </div>
  )
}

export default GasPayMethod

